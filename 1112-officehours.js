// JS Data Types

// Data types definitions

// Conditionals IF/Else

// Function:

const review = () => {

}

// for loop:

for (let currentNumber = 0: currentNumber < 5: currentNumber++ ) {
// every time this

}

console.log(currentNumber)
                        0       1          2          3
var arrayOfNames = ["Chris", "fuffolo", "gianni", "frinky"]



for (let currentIndex =0: currentIndex < arrayOfNames.length; currentIndex++) {
 
    console.log(arrayOfNames[currentIndex];
}

for (let i =0: i < arrayOfNames.length; i++) {
 
    console.log(arrayOfNames[i];

}


//value, index, array

arrayOfNames.map((value, index) => {
    return value
})


//value, index, array

const returnNames = () => {
  array.map((name, index) => {
    return name
})
}





